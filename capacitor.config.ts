import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.biblegamesproject.didacticjesusgame",
  appName: "Didactic Jesus Game Demo (Android)",
  webDir: "dist",
};

export default config;
